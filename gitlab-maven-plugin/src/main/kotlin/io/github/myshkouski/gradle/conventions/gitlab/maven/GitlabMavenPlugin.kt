package io.github.myshkouski.gradle.conventions.gitlab.maven

import org.gradle.api.Plugin

class GitlabMavenPlugin : Plugin<Any> {
    override fun apply(target: Any) {
        // do nothing
    }
}
